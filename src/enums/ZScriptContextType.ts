enum ZScriptContextType {
    Unknown,
    Class,
    Struct,
    Enum,
    String,
    Comment,
    Condition,
    ConditionParameters,
    Function,
    FunctionParameters,
}

export default ZScriptContextType;
